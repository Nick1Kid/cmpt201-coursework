#define _DEFAULT_SOURCE
#define _ISOC99_SOURCE

#include <stdlib.h>
#include <unistd.h>
#include <stdio.h>
#include <stdint.h>
#include <stddef.h>
#include <string.h>

struct header {
  uint64_t size;
  struct header* next;
};

int main() {
  void *base = sbrk(256);

  
